import { StrictMode } from 'react';
import { createRoot } from 'react-dom/client';
import App from './App.tsx';
import './index.js/index.ts';
import React from 'react'

function main() {
  return (
    <div>createRoot(document.getElementById('root')!).render(
       
      );</div>
  )
}

export default main
